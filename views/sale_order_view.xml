<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="sale_order_view_inherit">
            <field name="name">Sale Order View Inherit</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="model">sale.order</field>
            <field name="arch" type="xml">
                <xpath expr="//header" position="replace">
                    <header>
                        <field name="authorized_transaction_ids" invisible="1"/>

                        <button name="payment_confirmed" type="object" string="Confirmar pagamento"
                                attrs="{'invisible': [('state', 'in', ['done','cancel'])]}"/>
                        <button name="action_draft" type="object" string="Cancelar"
                                attrs="{'invisible': [('state', 'in', ['done'])]}"/>

                        <field name="state" widget="statusbar" statusbar_visible="draft,sent,sale"/>
                        <field invisible="1" name="company_id" options="{'no_create': True}"
                               groups="base.group_multi_company"/>
                    </header>
                </xpath>
                <xpath expr="//button[@name='preview_sale_order']" position="replace">

                </xpath>
                <xpath expr="//sheet/div[@name='button_box']" position="after">
                    <widget name="web_ribbon" title="Pago" bg_color="bg-success"
                            attrs="{'invisible': [('state', '!=', 'done')]}"/>
                </xpath>
                <xpath expr="//page[@name='customer_signature']" position="replace">

                </xpath>

                <xpath expr="//page[@name='other_information']" position="replace">

                </xpath>
            </field>
        </record>
    </data>
</odoo>